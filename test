public static void main(String [] args) {
     int x = 5;
     if (x > 0) {
         int y = x + 1;
     }
     System.out.println (y);

     int i = mystery (x);

    for (i = 0; i < 3; i++) {
        int z = i;
    }
    System.out.println (i);
 }

 public static int mystery (double y) {
     if (y < 0)
         return x * y;
     return y;
 }